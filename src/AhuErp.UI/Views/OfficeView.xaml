<UserControl x:Class="AhuErp.UI.Views.OfficeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:AhuErp.UI.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:OfficeViewModel}"
             d:DesignHeight="560" d:DesignWidth="900">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Отдел документационного обеспечения"
                   FontSize="20" FontWeight="SemiBold" Margin="0,0,0,16" />

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Documents}"
                  SelectedItem="{Binding SelectedDocument, UpdateSourceTrigger=PropertyChanged}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  RowBackground="Transparent">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background"
                            Value="{Binding Converter={StaticResource OverdueRowColorConverter}}" />
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="№" Binding="{Binding Id}" Width="40" />
                <DataGridTextColumn Header="Тип" Binding="{Binding Type, Converter={StaticResource EnumDisplayConverter}}" Width="120" />
                <DataGridTextColumn Header="Тема" Binding="{Binding Title}" Width="*" />
                <DataGridTextColumn Header="Создан" Binding="{Binding CreationDate, StringFormat=dd.MM.yyyy}" Width="100" />
                <DataGridTextColumn Header="Срок" Binding="{Binding Deadline, StringFormat=dd.MM.yyyy}" Width="100" />
                <DataGridTextColumn Header="Статус" Binding="{Binding Status, Converter={StaticResource EnumDisplayConverter}}" Width="120" />
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="2" Margin="0,16,0,0" Padding="16" Background="White" CornerRadius="4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="180" />
                    <ColumnDefinition Width="160" />
                    <ColumnDefinition Width="160" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Text="Тема" />
                <TextBlock Grid.Column="1" Text="Тип" Margin="12,0,0,0" />
                <TextBlock Grid.Column="2" Text="Статус" Margin="12,0,0,0" />
                <TextBlock Grid.Column="3" Text="Срок" Margin="12,0,0,0" />

                <TextBox Grid.Row="1"
                         Text="{Binding DraftTitle, UpdateSourceTrigger=PropertyChanged}"
                         Padding="6" />
                <ComboBox Grid.Row="1" Grid.Column="1"
                          ItemsSource="{Binding EditableTypes}"
                          SelectedItem="{Binding DraftType, UpdateSourceTrigger=PropertyChanged}"
                          ItemTemplate="{StaticResource EnumComboBoxItemTemplate}"
                          Margin="12,0,0,0" Padding="6" />
                <ComboBox Grid.Row="1" Grid.Column="2"
                          ItemsSource="{Binding Statuses}"
                          SelectedItem="{Binding DraftStatus, UpdateSourceTrigger=PropertyChanged}"
                          ItemTemplate="{StaticResource EnumComboBoxItemTemplate}"
                          Margin="12,0,0,0" Padding="6" />
                <DatePicker Grid.Row="1" Grid.Column="3"
                            SelectedDate="{Binding DraftDeadline, UpdateSourceTrigger=PropertyChanged}"
                            Margin="12,0,0,0" />

                <StackPanel Grid.Row="1" Grid.Column="4" Orientation="Horizontal" Margin="12,0,0,0">
                    <Button Content="Новый"  Command="{Binding NewCommand}"    MinWidth="80" Padding="8,4" Margin="0,0,6,0" />
                    <Button Content="Сохранить" Command="{Binding SaveCommand}"   MinWidth="90" Padding="8,4" Margin="0,0,6,0" />
                    <Button Content="Удалить"   Command="{Binding DeleteCommand}" MinWidth="80" Padding="8,4" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
