<UserControl x:Class="AhuErp.UI.Views.ArchiveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:AhuErp.UI.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ArchiveViewModel}"
             d:DesignHeight="560" d:DesignWidth="900">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Архивные запросы"
                   FontSize="20" FontWeight="SemiBold" Margin="0,0,0,16" />

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Requests}"
                  SelectedItem="{Binding SelectedRequest, UpdateSourceTrigger=PropertyChanged}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background"
                            Value="{Binding Converter={StaticResource OverdueRowColorConverter}}" />
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="№" Binding="{Binding Id}" Width="40" />
                <DataGridTextColumn Header="Тема" Binding="{Binding Title}" Width="*" />
                <DataGridTextColumn Header="Создан" Binding="{Binding CreationDate, StringFormat=dd.MM.yyyy}" Width="100" />
                <DataGridTextColumn Header="Срок" Binding="{Binding Deadline, StringFormat=dd.MM.yyyy}" Width="100" />
                <DataGridCheckBoxColumn Header="Паспорт" Binding="{Binding HasPassportScan}" Width="80" />
                <DataGridCheckBoxColumn Header="Трудовая" Binding="{Binding HasWorkBookScan}" Width="90" />
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="120" />
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="2" Margin="0,16,0,0" Padding="16" Background="White" CornerRadius="4">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Text="Тема" />
                <TextBox Grid.Row="1"
                         Text="{Binding DraftTitle, UpdateSourceTrigger=PropertyChanged}"
                         Padding="6" />

                <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="12,0,0,0">
                    <CheckBox Content="Скан паспорта"
                              IsChecked="{Binding DraftHasPassportScan, UpdateSourceTrigger=PropertyChanged}"
                              VerticalAlignment="Center" Margin="0,0,16,0" />
                    <CheckBox Content="Скан трудовой"
                              IsChecked="{Binding DraftHasWorkBookScan, UpdateSourceTrigger=PropertyChanged}"
                              VerticalAlignment="Center" />
                </StackPanel>

                <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,12,0,0">
                    <Button Content="Новый"     Command="{Binding NewCommand}"      MinWidth="80" Padding="8,4" Margin="0,0,6,0" />
                    <Button Content="Сохранить" Command="{Binding SaveCommand}"     MinWidth="90" Padding="8,4" Margin="0,0,6,0" />
                    <Button Content="Завершить" Command="{Binding CompleteCommand}" MinWidth="90" Padding="8,4" Margin="0,0,6,0" />
                    <Button Content="Удалить"   Command="{Binding DeleteCommand}"   MinWidth="80" Padding="8,4" />
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="#DC2626"
                               VerticalAlignment="Center" Margin="16,0,0,0" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
